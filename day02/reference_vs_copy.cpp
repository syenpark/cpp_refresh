/*
clang++ -std=c++20 -O0 -Wextra -g reference_vs_copy.cpp -o reference_vs_copy
clang++ -std=c++20 -O1 -g -fsanitize=address reference_vs_copy.cpp -o reference_vs_copy_asan
./reference_vs_copy_asan

*/

#include <iostream>
#include <vector>

struct Obj {
    int id;

    // Constructor
    Obj(int i) : id(i) {
        std::cout << "Construct " << id << "\n";
    }

    // Copy constructor (used when copying an object)
    Obj(const Obj& other) : id(other.id) {
        std::cout << "Copy construct " << id << "\n";
    }

    /*
    // ❌ WRONG - Infinite recursion!
    Obj(Obj other) : id(other.id) {
        std::cout << "Copy construct " << id << "\n";
    }

    // When you do: Obj b = a;
    // 1. Call copy constructor with 'a'
    // 2. To pass 'a' by value, we need to... COPY it!
    // 3. Which calls the copy constructor again
    // 4. Which needs to copy 'a' again
    // 5. Which calls the copy constructor...
    // ∞ INFINITE LOOP! Stack overflow!
    */

    // Destructor
    ~Obj() {
        std::cout << "Destruct " << id << "\n";
    }
};

// Function that takes an object by reference (no copy)
void f(const Obj& o) { // const to prevent modification
    // o is just another name for the same object argument passed in
    // No new object is created, no copy constructor called
    std::cout << "In f\n";
}

// Function that takes an object by value (copy happens)
void g(Obj o) {
    std::cout << "In g\n";
}

int main() {
    Obj a(1);       // Object 'a' is created on the stack

    f(a);           // No copy: 'a' is passed by reference to f
    g(a);           // Copy happens: 'a' is copied into 'o' in g

    // At the end of the function, destructors for 'a' and 'o' will be called
}

/*
═══════════════════════════════════════════════════════════════════════════
STATE 1: In main() - After Obj a(1) created
═══════════════════════════════════════════════════════════════════════════

Memory Layout:

HIGH ADDRESS (0xFFFF...)
┌─────────────────────────────────────┐
│          STACK                      │
│                                     │
├─────────────────────────────────────┤
│  main() stack frame                 │
│  ┌─────────────────────────────┐ ← Stack Pointer (SP)
│  │                             │    │
│  │  Obj a:                     │    │
│  │    id = 1     [4 bytes]     │    │  💚 "Construct 1" printed
│  │    address: 0xFF00          │    │
│  │                             │    │
│  │  return address             │    │
│  └─────────────────────────────┘    │
│                                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│          HEAP                       │
│  (no heap allocations)              │
└─────────────────────────────────────┘
LOW ADDRESS (0x0000...)


═══════════════════════════════════════════════════════════════════════════
STATE 2: Inside f(const Obj& o) - Pass by REFERENCE
═══════════════════════════════════════════════════════════════════════════

HIGH ADDRESS (0xFFFF...)
┌─────────────────────────────────────┐
│          STACK                      │
├─────────────────────────────────────┤
│  main() stack frame                 │
│  ┌─────────────────────────────┐    │
│  │  Obj a:                     │    │
│  │    id = 1     [4 bytes]     │    │  ← Original object
│  │    address: 0xFF00    ◄─────┼────┼─┐  still exists
│  │                             │    │ │
│  │  return address             │    │ │
│  └─────────────────────────────┘    │ │
├─────────────────────────────────────┤ │
│  f() stack frame                    │ │
│  ┌─────────────────────────────┐ ←  SP│
│  │  const Obj& o = 0xFF00      │    │ │  Reference (8 bytes)
│  │       │                     │    │ │  (just an alias/pointer)
│  │       └─────────────────────┼────┼─┘
│  │                             │    │    💡 No construction!
│  │  return address             │    │    💡 No new object!
│  └─────────────────────────────┘    │    💡 "In f" printed
│                                     │
└─────────────────────────────────────┘

           ╔═══════════════════════════════╗
           ║  REFERENCE - NO COPY          ║
           ║  • 'o' is just another name   ║
           ║    for 'a'                    ║
           ║  • Same memory location       ║
           ║  • Zero overhead              ║
           ║  • No constructor called      ║
           ╚═══════════════════════════════╝


═══════════════════════════════════════════════════════════════════════════
STATE 3: After f() returns - Back in main()
═══════════════════════════════════════════════════════════════════════════

HIGH ADDRESS (0xFFFF...)
┌─────────────────────────────────────┐
│          STACK                      │
├─────────────────────────────────────┤
│  main() stack frame                 │
│  ┌─────────────────────────────┐ ←  SP
│  │  Obj a:                     │    │
│  │    id = 1     [4 bytes]     │    │  ✅ Still alive
│  │    address: 0xFF00          │    │  ✅ No destructor called
│  │                             │    │
│  │  return address             │    │
│  └─────────────────────────────┘    │
│                                     │
│  (f's frame destroyed)              │  💡 No destructor because
│                                     │     no copy was made!
└─────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
STATE 4: Inside g(Obj o) - Pass by VALUE (COPY)
═══════════════════════════════════════════════════════════════════════════

HIGH ADDRESS (0xFFFF...)
┌─────────────────────────────────────┐
│          STACK                      │
├─────────────────────────────────────┤
│  main() stack frame                 │
│  ┌─────────────────────────────┐    │
│  │  Obj a:                     │    │  ← Original object
│  │    id = 1     [4 bytes]     │    │    still exists
│  │    address: 0xFF00          │    │
│  │                             │    │
│  │  return address             │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  g() stack frame                    │
│  ┌─────────────────────────────┐ ←  SP
│  │  Obj o:                     │    │  ← NEW object (copy)
│  │    id = 1     [4 bytes]     │    │    💚 "Copy construct 1"
│  │    address: 0xFE80          │    │    Different address!
│  │                             │    │
│  │  return address             │    │    💡 "In g" printed
│  └─────────────────────────────┘    │
│                                     │
└─────────────────────────────────────┘

           ╔═══════════════════════════════╗
           ║  PASS BY VALUE - COPY MADE    ║
           ║  • 'o' is a separate object   ║
           ║  • Different memory location  ║
           ║  • Copy constructor called    ║
           ║  • Uses more memory           ║
           ║  • Slower                     ║
           ╚═══════════════════════════════╝


      MEMORY COMPARISON:
      ┌──────────────┬──────────────┐
      │ Address      │  Object      │
      ├──────────────┼──────────────┤
      │ 0xFF00       │  a: id = 1   │  (original)
      │ 0xFE80       │  o: id = 1   │  (copy)
      └──────────────┴──────────────┘


═══════════════════════════════════════════════════════════════════════════
STATE 5: After g() returns - Back in main()
═══════════════════════════════════════════════════════════════════════════

HIGH ADDRESS (0xFFFF...)
┌─────────────────────────────────────┐
│          STACK                      │
├─────────────────────────────────────┤
│  main() stack frame                 │
│  ┌─────────────────────────────┐ ←  SP
│  │  Obj a:                     │    │
│  │    id = 1     [4 bytes]     │    │  ✅ Still alive
│  │    address: 0xFF00          │    │
│  │                             │    │
│  │  return address             │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ ❌❌❌ DESTROYED ❌❌❌     │    │  💀 "Destruct 1" printed
│  │  ~Obj() called for 'o'      │    │     (the copy in g)
│  │  Copy's destructor          │    │
│  └─────────────────────────────┘    │
│                                     │
└─────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
STATE 6: main() ends
═══════════════════════════════════════════════════════════════════════════

HIGH ADDRESS (0xFFFF...)
┌─────────────────────────────────────┐
│          STACK                      │
│                                     │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │ ❌❌❌ DESTROYED ❌❌❌     │    │  💀 "Destruct 1" printed
│  │  ~Obj() called for 'a'      │    │     (the original)
│  │  main's object              │    │
│  └─────────────────────────────┘    │
│                                     │
└─────────────────────────────────────┘
LOW ADDRESS (0x0000...)


═══════════════════════════════════════════════════════════════════════════
EXECUTION OUTPUT & EXPLANATION
═══════════════════════════════════════════════════════════════════════════

Console Output                         What Happens in Memory
─────────────────────────────────────────────────────────────────────────
Construct 1                            'a' created in main()
                                       → 1 object on stack

In f                                   'o' is just a reference to 'a'
                                       → Still 1 object total
                                       → No construction, no copy

Copy construct 1                       'o' is created as copy of 'a'
                                       → Now 2 objects on stack
                                       → Copy constructor called

In g                                   Inside g() function

Destruct 1                             'o' (copy) destroyed when g() ends
                                       → Back to 1 object

Destruct 1                             'a' (original) destroyed when main() ends
                                       → 0 objects


═══════════════════════════════════════════════════════════════════════════
COMPARISON TABLE
═══════════════════════════════════════════════════════════════════════════

┌─────────────────┬──────────────────────┬──────────────────────┐
│  Aspect         │  f(const Obj& o)     │  g(Obj o)            │
│                 │  PASS BY REFERENCE   │  PASS BY VALUE       │
├─────────────────┼──────────────────────┼──────────────────────┤
│ Memory Usage    │  8 bytes (pointer)   │  4 bytes (full copy) │
│ Construction    │  ❌ None             │  ✅ Copy constructor │
│ Objects Created │  0 (just alias)      │  1 (new copy)        │
│ Destruction     │  ❌ None             │  ✅ Destructor       │
│ Speed           │  ⚡ Very fast         │  🐢 Slower           │
│ Overhead        │  None                │  Copy cost           │
│ Can Modify?     │  No (const)          │  Yes (own copy)      │
│ Same Address?   │  ✅ Yes (0xFF00)     │  ❌ No (0xFE80)      │
└─────────────────┴──────────────────────┴──────────────────────┘


KEY INSIGHTS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. REFERENCE (const Obj& o):
   • 'o' is NOT a new object
   • 'o' is just another name for 'a'
   • Points to the same memory address
   • Zero construction/destruction overhead
   • Perfect for read-only operations

2. VALUE (Obj o):
   • 'o' IS a new object (independent copy)
   • Has its own memory address
   • Requires copy construction
   • Requires destruction
   • Use when you need to modify without affecting original

3. For large objects (like vectors with lots of data):
   • Pass by reference: just 8 bytes overhead
   • Pass by value: copies ALL data (could be megabytes!)
         
*/